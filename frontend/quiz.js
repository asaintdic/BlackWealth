class Quiz{
    constructor(question, choices, correct_choice, topic_id) {
        this.question = question
        this.choices = choices
        this.correct_choice = correct_choice
        this.topic_id = topic_id
    }
}

